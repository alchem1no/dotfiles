return {
    {
        "saghen/blink.pairs",
        enabled = false,

        version = "*",
        
        opts = {
            mappings = {
                enabled = true,
            },

            highlights = {
                enabled = true,
            },

            debug = false,
        },
    }
}
